import 'package:flutter/material.dart';

double ConstWidthScreen(BuildContext context) =>
    MediaQuery.of(context).size.width;
double ConstHeightScreen(BuildContext context) =>
    MediaQuery.of(context).size.height;
